import { containers, breakpoints, media } from './bootstrap-grid'

const colors = {
  black: '35, 31, 32',
  white: '255, 255, 255',
}

const boxShadows = {}

const theme = {
  breakpoints,
  media,
  colors,
  containers,
  boxShadows,
}

export default theme
